(function()  {
	"use strict";
	/*convention to create variable called app to declare
	assign to variable and pass in string and empty array*/
	var app = angular.module("templateTest", []);
	/* need to bind/tell Angular we want this module bound to our html
	need a single module that pushes everything else BY adding - ng-app in body tag of html*/


})();